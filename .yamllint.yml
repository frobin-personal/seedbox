extends: default

rules:
  line-length:
    max: 180
    level: warning
  truthy:
    level: warning
  comments:
    min-spaces-from-content: 1

ignore: |
  .git/
  services/generated/
  traefik/custom/
